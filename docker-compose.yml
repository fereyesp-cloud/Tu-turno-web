services:
  angular:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:80"
    depends_on:
      - json-server

  json-server:
    build:
      context: .
      dockerfile: Dockerfile.api
    ports:
      - "3000:3000"